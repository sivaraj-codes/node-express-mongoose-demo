{
  "name": "mongoose-demo",
  "version": "1.0.0",
  "description": "Express + Mongoose layered architecture demo",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "nodemon --exec \"node --env-file=.env\" server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "mongoose": "^8.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
